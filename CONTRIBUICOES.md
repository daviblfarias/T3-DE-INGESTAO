# Contribuições

Trabalho realizado individualmente.

| Integrante | Contribuições |
|---|---|
| [SEU NOME AQUI] | Configuração da pipeline (`config/`), extração MongoDB → Landing (`jobs/ingestion_job.py`), carga Landing → Bronze (`jobs/bronze_job.py`), tabelas de controle (`notebooks/00_setup_control_tables.py`), notificação via Telegram (`jobs/_telegram_notifier.py`), documentação (`README.md`, `docs/ARQUITETURA.md`) e execução das evidências (`docs/evidencias/`). |
